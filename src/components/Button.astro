---
const {
  text,
  data_aos = "focus-up",
  aria_label = "Button",
  class: customClass = "",
} = Astro.props;
---

<button
  class={`flex gap-2 justify-center font-nixel border border-white/10 tracking-[1px] items-center text-white/80 shadow-accent cursor-none bg-black shadow-accent-x hover:shadow-accent-animate rounded-full px-6 py-3 text-xl transition-all duration-1000 linear hover:scale-[1.01] hover:text-white hover:bg-accent ${customClass}`}
  data-aos={data_aos}
  aria-label={aria_label}
  style="word-spacing: -7px;"
>
  <slot />
  {text}
</button>
